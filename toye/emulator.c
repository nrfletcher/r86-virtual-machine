#include <stdio.h>

/* Entry into our emulator program
 * The emulator takes as input ToyISA (machine code), and executes
 * We will need to look at implementing registers, stack, pc, etc.
 */
int main() {
	return 0;
}
