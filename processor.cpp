#include <iostream>
#include <cstdint>
#include <vector>

std::vector<uint8_t> memory(64 * 1024);

std::vector<int> generate() {
    std::vector<int> vect;
    vect.push_back(2);
    return vect;
}

std::vector<int> doubleArr() {
    
}